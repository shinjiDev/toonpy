[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "toontools"
version = "0.5.0"
description = "Conversion utilities between JSON, YAML, TOML and TOON (Token-Oriented Object Notation)"
readme = "README.md"
authors = [
    { name = "shinjidev", email = "palomares.c@gmail.com" }
]
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.9"
dependencies = []
keywords = ["toon", "json", "yaml", "toml", "serialization", "parser", "data-format"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries",
    "Topic :: Utilities"
]

[project.urls]
Homepage = "https://github.com/shinjidev/toonpy"
Repository = "https://github.com/shinjidev/toonpy"
Documentation = "https://github.com/shinjidev/toonpy#readme"
"Bug Tracker" = "https://github.com/shinjidev/toonpy/issues"

[project.optional-dependencies]
tests = [
    "pytest>=7",
    "hypothesis>=6",
    "coverage>=7"
]
examples = [
    "tiktoken>=0.5.2"
]
yaml = [
    "PyYAML>=6.0"
]
toml = [
    "tomli>=2.0.0",
    "tomli-w>=1.0.0"
]

[project.scripts]
toonpy = "toonpy.cli:main"

[tool.setuptools.packages.find]
include = ["toonpy*"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.coverage.run]
source = ["toonpy"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = true

